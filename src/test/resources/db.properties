# DB clean before test run (Option B). Default: disabled so existing runs are unchanged.
# Set db.enabled=true and configure below to enable DB clean once before the first scenario.
db.enabled=false

# JDBC URL (must match the app's database). Examples:
# H2 (file):     jdbc:h2:file:./data/testdb
# H2 (mem):      jdbc:h2:mem:testdb
# MySQL:         jdbc:mysql://localhost:3306/qa_app
# PostgreSQL:    jdbc:postgresql://localhost:5432/qa_app
# db.url=jdbc:h2:file:./data/testdb

# db.user=sa
# db.password=

# Tables to truncate/delete (child tables first: sales -> plants -> categories).
# Comma-separated, no spaces. Add MySQL/PostgreSQL driver to pom.xml if not H2.
# db.tables=sale,plant,category
